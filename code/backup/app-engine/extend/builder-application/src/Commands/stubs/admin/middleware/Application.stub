<?php

namespace $MODULE_NAMESPACE$\$STUDLY_NAME$\Http\Middleware\Admin;

use Builder\Source\Admin\Admin;

class Application
{
    public function handle($request, \Closure $next, $app = 'source-$LOWER_NAME$-admin')
    {
        if ($app) {
            config(['builder-source-admin' => config($app)]);
        }

        return $next($request);
    }
}
